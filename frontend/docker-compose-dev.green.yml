version: "3"
services:
  frontend:
    build:
      dockerfile: Dockerfile # dockerfile이름
      context: ./ # 도커파일 위치 명시
    image: frontend-dev
    container_name: frontend-dev-green
    ports:
      - 3001:80
    stdin_open: true
